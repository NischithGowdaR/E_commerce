// MongoDB Collections are managed by Better Auth's MongoDB adapter
// Collections created automatically:
// - users
// - sessions
// - accounts
// - verifications
//
// Better Auth handles schema management for these collections automatically.
// No additional schema configuration is needed for MongoDB.

export const COLLECTIONS = {
  USERS: 'users',
  SESSIONS: 'sessions',
  ACCOUNTS: 'accounts',
  VERIFICATIONS: 'verifications',
}
